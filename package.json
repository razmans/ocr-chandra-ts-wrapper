{
  "name": "chandra-ts",
  "version": "0.1.0",
  "description": "TypeScript wrapper and auto-runtime client for Chandra OCR.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "chandra-ts": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "bridge",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "dev": "tsx src/cli/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "setup:local": "npm run build && node scripts/setup-local-runtime.mjs"
  },
  "keywords": [
    "ocr",
    "chandra",
    "typescript",
    "pdf",
    "markdown",
    "html"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  }
}
